{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "@resolvers/*": ["resolvers/*"],
      "@resolvers": ["resolvers/index"]
    }
  },
  "exclude": ["node_modules"],
  "include": ["src/**/*.ts"]
}
