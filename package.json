{
  "name": "futureproof",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "setup": "npm install",
    "postsetup": "npm run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "dependencies": {
    "@babel/runtime": "^7.17.8"
  }
}
