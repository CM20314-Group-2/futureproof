{
  "name": "futureproof",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "presetup": "lerna link",
    "setup": "lerna bootstrap --force-local && npm run build"
  },
  "private": true
}
