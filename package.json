{
  "name": "futureproof",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "postinstall": "yarn run build && lerna run postinstall"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "dependencies": {
    "@babel/runtime": "^7.17.8",
    "react-native-gesture-handler": "^2.3.2"
  }
}
